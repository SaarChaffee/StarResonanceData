syntax = "proto3";

package zproto;

import "enum_e_error_code.proto";

message GetMailManagerReply {
    int32 normalNum = 1;
    repeated int64 normalUnReadList = 2;
    int32 collectNum = 3;
    repeated int64 collectUnReadList = 6;
    zproto.EErrorCode errCode = 7;
}
