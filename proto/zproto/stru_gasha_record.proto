syntax = "proto3";

package zproto;

import "stru_gasha_record_item_info.proto";

message GashaRecord {
    string charIdPool = 1;
    uint32 poolId = 2;
    repeated zproto.GashaRecordItemInfo gashaRecordInfo = 3;
    int64 firstTimestamp = 4;
}
