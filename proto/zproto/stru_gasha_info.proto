syntax = "proto3";

package zproto;

import "stru_dirty_mask.proto";

message GashaInfo {
    uint32 id = 1;
    uint32 drawCount = 4;
    int64 refreshTime = 5;
    map<int32, message> dirty = 1000;
}

